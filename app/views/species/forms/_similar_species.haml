= f.label(:similar_species_id, "Similar Species ID")
= f.text_field(:similar_species_id, :size => "5")
    
- unless f.object.new_record?
  = f.check_box('_destroy')
  = f.label('_destroy', 'Remove')
%br