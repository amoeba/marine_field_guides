%h2 Showing all Species
%ul
  - @species.each do |s|
    %li
      = link_to(s.scientific_name, species_instance_path(s))
      = link_to("edit", edit_species_instance_path(s), :class => "action")
      = link_to("delete", species_instance_path(s), :method => 'delete', :confirm => "Are you sure?", :class => "action")